# Catai

Vue 3 + TypeScript starter scaffolded for Vite and VS Code Dev Containers.

## Quick start

```bash
npm install
npm run dev
```

## Dev Container

Open the repository in VS Code and choose **Reopen in Container**. The container installs dependencies on first create and forwards port `5173`.